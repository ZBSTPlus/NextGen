{
    "root": true,
    // "parser": "vue-eslint-parser", // Use a parser that supports HTML parsing
    // "parserOptions": {
    //   "parser": "html" // Specify HTML parsing mode
    // },
    "extends": [
      "eslint:recommended" // Base set of recommended rules
    ],
    "rules": {
      // Adjust these rules based on your preferences and project needs
      "semi": ["error", "always"], // Enforce semicolons
      "quotes": ["error", "double"] // Enforce double quotes
      // "no-console": "warn", // Warn about console usage
      // "vue/script-indent": ["error", 2, { "baseIndent": 1 }], // Enforce consistent indentation for inline scripts
    },
    "overrides": [
      {
        "files": ["*.html"],
        "rules": {
          // Disable any rules that are not applicable to inline JavaScript
          // (e.g., rules related to module imports or exports)
        }
      }
    ]
  }
  